# Models

Exported, quantized keyword-spotting models from Edge Impulse land here.

Each model version should be committed alongside a short note on:
- Training dataset size (samples per speaker, noise/negative class composition)
- Reported accuracy and false-activation rate
- Measured on-device footprint (RAM/CPU) — not simulated
